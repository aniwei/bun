{
  "name": "bun-browser",
  "version": "0.0.0",
  "private": true,
  "description": "Bun Core compiled to WebAssembly for browsers, driven by a host JS engine via the JSI bridge.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./kernel": "./src/kernel.ts",
    "./kernel-worker": "./src/kernel-worker.ts",
    "./jsi-host": "./src/jsi-host.ts",
    "./vfs-client": "./src/vfs-client.ts",
    "./protocol": "./src/protocol.ts"
  },
  "files": [
    "src/**/*.ts",
    "bun-core.wasm",
    "README.md"
  ],
  "scripts": {
    "build:wasm": "cd ../.. && /tmp/zig-dl/bootstrap-aarch64-linux-musl/zig build --build-file build-wasm-smoke.zig build-wasm && cp zig-out/packages/bun-browser/bun-core.wasm packages/bun-browser/bun-core.wasm",
    "build:wasm:release": "cd ../.. && /tmp/zig-dl/bootstrap-aarch64-linux-musl/zig build --build-file build-wasm-smoke.zig build-wasm -Doptimize=ReleaseFast && cp zig-out/packages/bun-browser/bun-core.wasm packages/bun-browser/bun-core.wasm",
    "build:wasm:opt": "cd ../.. && /tmp/zig-dl/bootstrap-aarch64-linux-musl/zig build --build-file build-wasm-smoke.zig build-wasm -Doptimize=ReleaseFast && wasm-opt -O3 --enable-bulk-memory --strip-debug --strip-producers -o packages/bun-browser/bun-core.wasm zig-out/packages/bun-browser/bun-core.wasm",
    "build:demo": "bun demo/build.ts",
    "demo:serve": "bun run build:demo && bunx serve demo/dist",
    "typecheck": "tsc -p .",
    "test": "bun test test/"
  },
  "devDependencies": {
    "@types/node": "^20.4.10",
    "typescript": "^6.0.0"
  }
}
