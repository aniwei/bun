# Phase 3: API 覆盖与稳定性

- 状态: In Progress (Phase 3 prework started)
- 总完成百分比: 87%
- 测试状态: Pass (`bunx tsc -p tsconfig.json --noEmit && bunx oxlint --max-warnings 0 && bun test packages/mars-test/src/`: 151 pass / 0 fail / 991 expect calls)
- 依赖 Phase: Phase 1、Phase 2 已通过
- 执行策略: Phase 3 已开始推进低风险预研切片；当前优先把命令层、Kernel pid、stdio、ServiceWorker/Worker 边界逐段补齐，并保持 Phase 1/2 验收不回退。
- 对应技术设计文档:
  - `mars-lib/rfc/0001-mars-lib-technical-design.md` - 系统设计和 API 规范
  - `mars-lib/rfc/0002-hook-system-design.md` - Hook 系统时序、设计决策和新功能清单
- Playground 接入矩阵: `mars-lib/playground/README.md`
- Playground 功能模块用例: `mars-lib/playground/module-cases.json`
- 相关章节: 8 MarsKernel 进程与端口接口, 9 MarsVFS 接口, 10 Mars Service Worker Runtime, 11 Bun API Facade, 16 Vite + React + TypeScript 支持, 18 MarsCore Rust/WASM 边界, 20.6 浏览器兼容, 22 M3, 23 风险与对策

## 开始前确认

### 技术设计核对

- 确认 Phase 3 不改变 Phase 1/2 的验收主线，只在现有 MarsRuntime、MarsKernel、MarsVFS、MarsLoader、MarsBundler 基础上扩展 API 覆盖率和稳定性。
- 确认 `Bun.spawn()` 在浏览器中映射为受控 Worker 或内置命令，不承诺 native 子进程。
- 确认 `Bun.spawnSync()` 在无 SharedArrayBuffer 环境下允许降级为明确错误或 async fallback，具体策略需要记录在未决问题中。
- 确认 `Bun.build()` 可以先复用 MarsBundler 的 `esbuild-wasm` transform 能力，输出到 MarsVFS。
- 确认运行时转译和 build 转译分工明确: MarsTranspiler/MarsLoader 继续通过 `@swc/wasm-web` 执行 TS/TSX/JSX，`Bun.build()` 预研切片独立使用 `esbuild-wasm` 输出构建产物。
- 确认 `bun run index.ts` 先作为 MarsShell 命令层切片接入当前内存态 Kernel pid、`runEntryScript()`、SWC WASM loader 和 stdio bridge；后续再替换为 Kernel Worker + Process Worker 隔离模型。
- 确认真实 ServiceWorker 链路仍未完成: 当前已具备 `fetch` event handler、registration/bootstrap、client/SW bridge、真实 playground SW scope smoke、页面级 SW/SAB 状态面板、`/src/*.ts(x)` 源码 URL 首跳、VFS `node_modules` bare import 二跳、嵌套 npm package graph、package exports subpath graph、scoped exports subpath graph、Vite host network fallback（含 env、virtual module、source query 变体与 optimized dependency URL），以及 SW/Process Worker VFS 自动 fanout 切片；npm graph 边缘案例与 Vite special path 完整接管尚未完成。
- 确认 crypto/password 可以优先使用 WebCrypto 或 Rust crypto wasm，所有输入输出类型需与 Bun 常见用法兼容。
- 确认 sqlite 先以 MarsVFS-backed SQL prework 锁定 `Bun.sql` facade、database 文件持久化和常见查询验收；后续替换为 sqlite WASM 模块。
- 确认 WebSocket upgrade 经由 Service Worker/虚拟服务器通道实现，浏览器安全限制下不提供原生 TCP。
- 确认 OPFS 持久化用于恢复 runtime 文件状态，不替代内存层热路径。
- 确认 Chrome 与 Firefox 验收要分别覆盖 SAB 与 async fallback profile。

### 代码风格

- 以 `mars-lib/AGENTS.md` 为默认基线，若子模块已有规范且冲突，需在变更说明中注明偏差理由。
- TypeScript / JavaScript 尽量不使用分号 `;` 和双引号 `"`，避免超长行，复杂表达式主动换行。
- 命名保持语义完整，除极小作用域临时变量外，不使用 `tmp`、`ctx`、`cfg`、`obj` 等泛化命名。
- 导入顺序遵循 AGENTS 规则: 绝对 default -> 相对 default -> 绝对具名 -> 相对具名 -> type imports -> 样式/静态资源。
- 新 API 必须同步维护兼容矩阵，明确 supported/partial/unsupported/fallback 行为。
- crypto/sqlite/websocket 等能力保持独立包实现，公共类型与实现拆分，避免 `mars-runtime` 单点膨胀。
- 单文件超过 1000 行必须拆分，优先按领域职责、运行时边界（runtime/kernel/vfs/loader/hooks）、types 与实现拆分。
- Rust 代码遵循同等可读性要求，`use` 顺序为标准库/第三方 -> `crate::` -> `super::`。
- 提交前执行并通过 `ox format --check` 与 `ox lint --max-warnings 0`。

### 前置依赖

- Phase 1: Runtime、VFS、Kernel、SW、Shell、Express/Koa 验收通过。
- Phase 2: Resolver、`@swc/wasm-web` Transpiler、Loader、Installer、Vite React TS、TSX 执行、playground 矩阵与文档同步验收通过。

## Todo

| ID | 状态 | 完成百分比 | 测试通过 | 文件/模块 | 功能介绍 | 完成时设计核对 |
| --- | --- | ---: | --- | --- | --- | --- |
| M3-01 | In Progress | 94% | Pass | `mars-lib/packages/mars-runtime/src/compat-matrix.ts` | 定义 Bun API 兼容矩阵，记录 supported/partial/unsupported/fallback。 | 已记录 M1/M3 已实现 API 与 planned API，并由 Phase3 测试覆盖 Bun.build/minify/hash、bun run entry/package script pre/post lifecycle、参数透传与 npm script env/PATH、Bun.spawn、Shell/runtime.spawn/Bun.spawn configured Process Worker route、spawnSync fallback、CryptoHasher digest encodings（含 sha384/copy/digestSync）、Bun.password cost mapping/binary input/malformed hash、node:crypto digest encoding（含 sha384）与 Bun.sql（含 prepare）条目；本轮同步 Bun.spawn/Bun.spawnSync stdin-backed `cat`、`bun run <script>` package.json scripts、`pre<script>`/`post<script>` lifecycle、`--` 参数透传、`npm_lifecycle_*`/`npm_package_*`/`node_modules/.bin` PATH、Bun.spawnSync VFS-backed ls/cat/grep/mkdir/rm/cwd-aware pwd、Bun.password malformed Mars hash 稳定返回 false、node:crypto hkdf/getCiphers/getCurves/stubs、http/node:http core module 注入、官方 npm express/koa fixture 运行边界、nested npm dependency graph、capabilities crossOriginIsolated/broadcastChannel/indexedDB 说明；后续新增 API 需持续补全。 |
| M3-02 | In Progress | 91% | Pass | `mars-lib/packages/mars-kernel/src/process-worker-factory.ts`, `mars-lib/packages/mars-kernel/src/kernel-worker-bootstrap.ts`, `mars-lib/packages/mars-kernel/src/kernel-worker-client.ts`, `mars-lib/packages/mars-kernel/src/kernel.ts`, `mars-lib/packages/mars-runtime/src/process-worker-bootstrap.ts`, `mars-lib/packages/mars-runtime/src/process-worker-script.ts` | 实现受控 Process Worker 创建、初始化、销毁。 | 已覆盖受控 worker boot/message/terminate 生命周期、无 worker URL 的 in-memory fallback、Worker URL + `new Worker()` native carrier、Kernel Worker `kernel.connect` bootstrap 到 Process Worker lifecycle/VFS patch/run RPC、页面侧 Kernel Worker `new Worker()` carrier、`kernel.spawn({ kind: "worker" })` 自动创建 Process Worker 并将 stdin/stdout/stderr/exit 通过 controller 桥接到 Kernel pid、Process Worker runtime bootstrap 的 `Bun`/`process`/`require` 上下文注入、module Worker bootstrap source/Blob URL 生成、factory 绑定源 VFS 后的自动 write/delete fanout，以及 playground 真实浏览器 Blob module Worker smoke；新增 `runtime.dispose()` 主动终止 active native Process Worker carrier 并收敛 `ProcessHandle.exited`；script loading 自动化待补。 |
| M3-03 | In Progress | 87% | Pass | `mars-lib/packages/mars-kernel/src/stdio.ts`, `mars-lib/packages/mars-kernel/src/kernel.ts`, `mars-lib/packages/mars-kernel/src/process-worker-factory.ts`, `mars-lib/packages/mars-runtime/src/process-worker-bootstrap.ts` | 实现 stdin/stdout/stderr stream 桥接、backpressure 和关闭语义。 | 已覆盖 ProcessHandle stdin readable、`write()` 输入、`closeStdin()` 输入、初始 string/ReadableStream `stdin` 输入、初始 stdin stream close、stdout/stderr readable、WritableStream mirror，补齐 Process Worker native carrier 的 `process.worker.stdin` / `process.worker.stdin.close` / stdout/stderr/exit 消息协议，让 configured Process Worker spawn 将 `ProcessHandle.write()` / `ProcessHandle.closeStdin()` 与初始 stdin 转发到 worker，并在初始 stdin 结束时关闭 worker 内部 pid stdin；新增 closeStdin 后写入会返回明确错误，`kernel.spawn({ kind: "worker" })` 经 Kernel Worker RPC 自动创建 Process Worker 时也会转发 stdin chunk 与 close 语义；完整 streaming backpressure 精细语义待补。 |
| M3-04 | In Progress | 82% | Pass | `mars-lib/packages/mars-runtime/src/install-global.ts`, `mars-lib/packages/mars-client/src/runtime.ts`, `mars-lib/packages/mars-shell/src/commands/fs.ts` | 实现 `Bun.spawn()` 到 Process Worker 或内置命令的映射。 | 当前 `Bun.spawn({ cmd: ["bun", "run", "index.ts"] })`、`runtime.spawn("bun", ["run", "index.ts"])` 与通用 shell 命令（如 `echo`、stdin-backed `cat`）均可复用内存态 Kernel pid，命令结果会回灌到 ProcessHandle stdout/stderr/exit；新增 native-capable Process Worker factory 配置路径，`runtime.spawn("bun", ["run", "index.ts"])` 与 `Bun.spawn({ cmd: ["bun", "run", "index.ts"] })` 均可创建 Process Worker、转发 ProcessHandle `write()` / `closeStdin()`、初始 string stdin、初始 ReadableStream stdin、初始 stdin close，并回灌 worker stdout/stderr/exit；新增 `Bun.spawn("bun", "run", "index.ts")` shorthand argv 路径经同一 configured Process Worker 通道执行；本轮补齐 Shell `cat` 对初始 string/ReadableStream stdin 的消费，并让 `Bun.spawn({ cmd: ["bun", "run", "<script>"] })` 复用 package script 路径，完整 backpressure 细节待补。 |
| M3-05 | In Progress | 81% | Pass | `mars-lib/packages/mars-runtime/src/install-global.ts`, `mars-lib/packages/mars-runtime/src/types.ts` | 实现 `Bun.spawnSync()` 的 SAB 路径和无 SAB 降级错误。 | 已补 capability-aware fallback：无 SAB/Atomics.wait 时返回明确 requirement 错误；SAB 可用时扩展内置同步命令集（`echo`、`true`、`false`、cwd-aware `pwd`、`printf`、空 stdin `cat`、string stdin-backed `cat`、VFS-backed `ls`/`cat`/`grep`、VFS mutation `mkdir`/`rm` 均可返回同步 stdout/stderr/exitCode）；未知命令、ReadableStream stdin、缺失 VFS 路径与 mutation 失败会返回明确错误，并保留 `RuntimeContext.spawnSync` 扩展点；完整通用进程执行路径待补。 |
| M3-06 | In Progress | 82% | Pass | `mars-lib/packages/mars-bundler/src/build.ts` | 实现 `Bun.build()` 底层构建入口，支持 entrypoints/outdir/outfile/format/target。 | 已改用 `esbuild-wasm` transform，支持 entrypoints、outfile/outdir、format、target、define、sourcemap、minify 与 VFS 读取；可构建 vite playground entry；完整依赖 bundling/splitting 待补。 |
| M3-07 | In Progress | 84% | Pass | `mars-lib/packages/mars-bundler/src/output-writer.ts` | 将 build 产物、source map、logs 写入 MarsVFS。 | 已写入 MarsVFS、自动创建输出目录，并返回 text/arrayBuffer artifact；`sourcemap: true` 会额外写出 `.js.map` source-map artifact；新增 `hash` 字段（SHA-256 前16字符十六进制）随产物一起写出。完整 BuildResult artifact 元数据待补。 |
| M3-08 | In Progress | 80% | Pass | `mars-lib/packages/mars-runtime/src/bun-build.ts`, `mars-lib/packages/mars-bundler/src/types.ts` | 将 `Bun.build()` facade 接到 MarsBundler。 | `runtime.bun.build()` 已接入 MarsBundler 的 esbuild 路径并覆盖 TSX entry 验收；`BuildOutputArtifact` 新增可选 `hash` 字段（SHA-256 前16字符十六进制），产物写入时自动计算并填充；Bun BuildResult 完整结构待补。 |
| M3-09 | In Progress | 83% | Pass | `mars-lib/packages/mars-crypto/src/hasher.ts`, `mars-lib/packages/mars-runtime/src/install-global.ts` | 实现 `Bun.CryptoHasher` 常用算法，优先 sha1/sha256/sha512/md5。 | 已通过 WebCrypto 覆盖 sha1/sha256/sha384/sha512 async digest，并为 md5 补 Mars fallback；覆盖 hex/base64/base64url/buffer 输出编码；新增 `copy()` 克隆 hasher 状态（chunk 深拷贝）；新增 `digestSync()` ：md5 纯 JS 同步返回结果，sha* 算法抛出明确 `sync not available` 错误；接入 `Bun.CryptoHasher`、`createHashDigest`、`node:crypto.createHash` 和 playground fixture；完整 sha* 同步 digest 待补。 |
| M3-10 | In Progress | 72% | Pass | `mars-lib/packages/mars-crypto/src/password.ts`, `mars-lib/packages/mars-runtime/src/install-global.ts` | 实现 `Bun.password.hash()`、`verify()` 的浏览器兼容路径。 | 已通过 WebCrypto PBKDF2-SHA256 fallback 覆盖 hash/verify、string/binary 输入、显式 iterations 和 cost-to-iterations 映射；新增 bcrypt ($2y$/$2b$/$2a$) 和 argon2 ($argon2) 格式识别，`verify()` 遇到这两种格式时抛出明确的 `not supported in browser context` 错误而非静默返回 false，`hash()` 传入 `algorithm: "bcrypt"/"argon2id"/"argon2d"/"argon2i"` 也抛出明确 `not available` 错误；malformed `$mars$pbkdf2-sha256` hash 会稳定返回 false 而非泄露 base64 解码错误；Bun 原生 bcrypt/argon2 实现待补。 |
| M3-11 | In Progress | 83% | Pass | `mars-lib/packages/mars-node/src/crypto.ts` | 实现 `node:crypto` 验收路径子集，如 createHash、createHmac、randomUUID、randomBytes。 | 已覆盖 randomUUID、randomBytes、sha/md5 async createHash digest、同步 SHA-1 `createHash().digest()`（供官方 Express etag 路径使用）、async createHmac digest 与常见 digest encoding；新增 `getHashes()` 返回支持算法列表、`timingSafeEqual(a, b)` 恒时字节比较（纯 JS）、`pbkdf2(password, salt, iterations, keylen, digest, callback)` 回调接口与 `pbkdf2Async(...)` Promise 接口（均通过 WebCrypto PBKDF2 实现）；新增 `hkdf(hash, key, salt, info, keylen, callback)` 回调接口与 `hkdfAsync(...)` Promise 接口（WebCrypto HKDF）、`getCiphers()` 返回空列表、`getCurves()` 返回 P-256/P-384/P-521；`createSign/createVerify/createCipheriv/createDecipheriv/hkdfSync` 抛明确 browser-context 错误；完整同步 Hash/Hmac、cipher/sign/verify 和 scrypt 待补。 |
| M3-12 | In Progress | 88% | Pass | `mars-lib/packages/mars-sqlite/src/sqlite.ts` | 加载 sqlite WASM，初始化数据库实例。 | 已切到 `sql.js`（sqlite WASM）引擎并移除 JS prework fallback，实现 wasm-only 初始化与执行路径。 |
| M3-13 | In Progress | 84% | Pass | `mars-lib/packages/mars-sqlite/src/sqlite.ts` | 将 sqlite 文件读写适配到 MarsVFS。 | 已将数据库以 binary 形式持久化到 MarsVFS（默认 `.sqlite` 路径），覆盖 reopen 读取、事务 commit/rollback 语义与 sqlite 文件 header 校验（playground wasm case）；OPFS 文件锁与多 worker 并发写入仍待补。 |
| M3-14 | In Progress | 84% | Pass | `mars-lib/packages/mars-runtime/src/install-global.ts`, `mars-lib/packages/mars-runtime/src/types.ts`, `mars-lib/packages/mars-sqlite/src/sqlite.ts`, `mars-lib/playground/core-modules/runtime/sqlite-transaction.ts`, `mars-lib/playground/core-modules/runtime/sqlite-wasm.ts` | 提供 `Bun.sql` 或 SQLFactory facade。 | 已接入 `Bun.sql` tagged query、`Bun.sql.db` 默认 database、`Bun.sql.open(path)`、BEGIN/COMMIT/ROLLBACK 验收；新增 `db.prepare(sql)` 返回 `MarsSQLPreparedStatement`，支持 `all(params)/get(params)/run(params)/finalize()` 完整生命周期和 finalize 后抛错保护；接入 playground sqlite/sqlite-transaction/sqlite-wasm 用例；完整 Bun sqlite 同步 API 与更高阶 SQL 兼容待补。 |
| M3-15 | Done | 100% | Pass | `mars-lib/packages/mars-runtime/src/websocket-pair.ts`, `mars-lib/packages/mars-runtime/src/bun-serve.ts`, `mars-lib/packages/mars-runtime/src/types.ts` | 实现 VirtualServer WebSocket upgrade 适配层。MarsServerWebSocketImpl/MarsClientWebSocket 直接回调对 + createWebSocketPair/upgradeToWebSocket；server.upgrade(request) 通过 Upgrade 头检测返回 true 并触发 websocket.open/message/close 回调；ServeOptions 扩展 websocket?: WebSocketHandlerOptions<T>。 | 已覆盖 createWebSocketPair 双向消息/关闭、upgradeToWebSocket open/message/close 生命周期、server.close 后 readyState 同步传播，验收测试全通过。 |
| M3-16 | Done | 100% | Pass | `mars-lib/packages/mars-sw/src/websocket-route.ts`, `mars-lib/packages/mars-sw/src/classify-request.ts`, `mars-lib/packages/mars-sw/src/router.ts` | 在 Service Worker 中分发 WebSocket upgrade 请求：ws://mars.localhost URL 被 classifyRequest 识别为 "websocket" kind，ServiceWorkerRouter.handle() 返回 426 Upgrade Required 诊断响应（Service Worker 无法拦截原生 WebSocket 升级，客户端需改用 MarsClientWebSocket in-process 通道）；handleWebSocketRoute 返回带 x-mars-ws-port/pathname 诊断头的 426。 | 已覆盖 ws/wss 协议分类、外部 ws URL fallback、ServiceWorkerRouter websocket handler、426 响应头断言，验收测试全通过。 |
| M3-17 | In Progress | 76% | Pass | `mars-lib/packages/mars-vfs/src/opfs-adapter.ts` | 实现 OPFS persistence adapter，支持 open/get/set/delete/keys/close。 | 已覆盖 open/get/set/delete/keys/close、无 OPFS 环境 memory fallback，以及可用 OPFS scope 场景下的 namespace/read/write/delete/keys/has/size/clear 行为；新增 `has(key)`（O(1) 存在性检查）、`size()`（条目数量）、`clear()`（清空全部条目）方法，MemoryPersistenceAdapter 和接口均同步更新，并验证 OPFS namespace 隔离；真实浏览器 OPFS profile 自动化待补。 |
| M3-18 | In Progress | 88% | Pass | `mars-lib/packages/mars-vfs/src/snapshot.ts`, `mars-lib/packages/mars-vfs/src/patch.ts`, `mars-lib/packages/mars-runtime/src/process-worker-script.ts`, `mars-lib/playground/vite.config.ts` | 实现 snapshot/restore，支持 runtime 文件状态恢复。 | 已覆盖 FileTree JSON-safe base64 序列化、跨 runtime restore，并接入 Process Worker bootstrap script 与真实 ServiceWorker scope smoke 的启动态 VFS hydrate；新增 JSON-safe VFS patch 协议，Process Worker runtime 可在 boot 后应用增量 write/delete patch 再显式 run，ServiceWorker bridge 可通过 `sw.vfs.patch` 更新 scope 内 module VFS，`MarsRuntime` 已能用 VFS watcher 将 `Bun.write()` / `vfs.writeFile()` 自动 fanout 到 ServiceWorker VFS，`MarsProcessWorkerFactory` 也可绑定源 VFS 自动 fanout 到 Process Worker VFS。增量 snapshot 和文件元数据待补。 |
| M3-19 | In Progress | 79% | Pass | `mars-lib/packages/mars-kernel/src/capabilities.ts`, `mars-lib/playground/vite.config.ts`, `mars-lib/playground/src/App.tsx` | 检测 SharedArrayBuffer、Atomics.wait、OPFS、Service Worker 等能力。 | 已覆盖 ServiceWorker、SharedArrayBuffer、Atomics.wait、OPFS、WebCrypto、Worker 检测，并让 playground dev/preview 输出 COOP/COEP/CORP headers 进入 `crossOriginIsolated`；页面状态面板会自动启动真实 ServiceWorker 并显示 SAB/SW ready 状态；新增 `crossOriginIsolated`（scope.crossOriginIsolated）、`broadcastChannel`（typeof BroadcastChannel === "function"）、`indexedDB`（Boolean(scope.indexedDB)）三个能力字段；Chrome/Firefox 自动化 profile 待接。 |
| M3-20 | In Progress | 92% | Pass | `mars-lib/packages/mars-test/src/browser-profiles.ts`, `mars-lib/packages/mars-test/src/browser-automation.ts` | 定义 chromium/firefox、sab/async fallback、memory/opfs 测试 profile。 | 已定义 async fallback、SAB worker、OPFS persistence、ServiceWorker module profiles，并新增 Chromium/Firefox 自动化矩阵元数据（Chromium SAB+SW、Chromium OPFS、Firefox async fallback、Firefox SW modules）；新增 `createBrowserAutomationRunPlan()` 生成 runner command/args/env（按 engine project + profile grep）、`runBrowserAutomationPlan()` 执行并汇总 profile 级 exitCode/stdout/stderr/耗时与 stop-on-failure，以及 `createBunSpawnBrowserAutomationExecutor()` 的 Bun.spawn 执行器接线与无 Bun runtime 显式错误路径；已由 Phase 3 验收覆盖 run-plan、执行汇总与 Bun.spawn 执行器路径。 |
| M3-21 | In Progress | 99% | Pass | `mars-lib/packages/mars-test/src/phase3.acceptance.test.ts` | 覆盖重复 boot/dispose、spawn/kill、build、crypto、sqlite、OPFS restore。 | 已覆盖 Bun facade version 读取 package version、Bun.build、Bun.build external source map artifact、Bun.build minify、bun run entry 与 package scripts pre/post lifecycle/参数透传/npm script env/PATH bin lookup、Shell/runtime.spawn/Bun.spawn configured Process Worker route、ProcessHandle stdin forwarding、ProcessHandle closeStdin forwarding、initial stdin forwarding、stdin stream close、Bun.spawn、Bun.spawn/runtime.spawn generic shell stdin cat、spawnSync fallback/stdin cat、spawnSync VFS-backed ls/cat/grep/mkdir/rm、stdio bridge、process worker factory、Process Worker native carrier + stdio protocol、Process Worker runtime bootstrap context 注入和 `bun run` module stdout/exit、Process Worker bootstrap script source/Blob URL 生成、bootstrap script snapshot restore、Process Worker runtime incremental VFS patch 后显式 run、Process Worker factory VFS write 自动 fanout、Kernel Worker bridge 到 Process Worker patch/run RPC、ServiceWorker bridge incremental VFS patch 后 module fetch、runtime VFS write 自动 fanout 到 ServiceWorker bridge、playground 真实浏览器 Worker smoke 登记、真实 ServiceWorker scope smoke（含 native `fetch()` 经 ServiceWorker fetch event）登记、playground host SAB/SW 状态登记、SW/Kernel/Process bridge prework、ServiceWorker fetch event handler、ServiceWorker registration lifecycle、ServiceWorker bootstrap connect、ServiceWorker ESM module response、`/src/*.ts` 源码 URL 首跳、VFS `node_modules` bare import 二跳、嵌套 npm package graph、package exports subpath graph、scoped exports subpath graph 与 Vite host network fallback（含 env、virtual module、source query 变体与 optimized dependency URL）、Kernel Worker bootstrap connect、Kernel Worker native carrier、MessageChannel transport、CryptoHasher algorithms/digest encodings、Bun.password hash/verify/cost mapping、node:crypto createHash/createHmac/digest encoding、官方 npm-installed express/koa/node:http Phase 2 集成路径、Bun.sql MarsVFS-backed sqlite prework、VFS snapshot restore、OPFS adapter、capabilities/profile、Chromium/Firefox automation profile matrix、browser automation run-plan 与执行汇总（含 stop-on-failure 与 Bun.spawn executor）接线输出、vite playground entry 与兼容矩阵。 |
| M3-22 | In Progress | 99% | Pass | `mars-lib/docs/compatibility/bun-api.md` | 生成或维护 Bun API 兼容矩阵文档。 | 已记录执行拓扑、stdio bridge、process worker factory、Process Worker runtime bootstrap context 注入、SW/Kernel/Process bridge prework、ServiceWorker fetch event handler、ServiceWorker registration/bootstrap、ServiceWorker ESM module response、Kernel Worker bootstrap/native carrier、MessageChannel transport、Bun.build/minify/hash、bun run entry/package scripts、pre/post lifecycle、参数透传、npm script env 与 PATH bin lookup、Shell/runtime.spawn/Bun.spawn configured Process Worker route、Bun.spawn generic shell stdin、spawnSync fallback/VFS-backed reads/mutations/stdin cat、CryptoHasher digest encodings、Bun.password cost mapping/binary input/malformed hash、node:crypto digest/HKDF、http/node:http core module 注入、官方 npm fixture express/koa 运行边界、nested dependency install、Bun.sql prepare、VFS snapshot、OPFS adapter has/size/clear、capabilities/profile 当前范围；后续 API 需同步维护。 |
| M3-23 | In Progress | 86% | Pass | `mars-lib/packages/mars-client/src/runtime.ts`, `mars-lib/playground/core-modules/runtime/bun-run-index.ts` | 将 `bun run index.ts` 接到 MarsShell 命令层、Kernel 虚拟 pid、`runEntryScript()` 和 stdio bridge。 | 已覆盖 shell、runtime.spawn、Bun.spawn 三条入口的 playground fixture 与 Phase 3 验收，并补齐 Kernel stdio bridge 前置；新增 configured native Process Worker factory 路径，Shell `bun run`、`runtime.spawn("bun", ["run", "index.ts"])` 与 `Bun.spawn({ cmd: ["bun", "run", "index.ts"] })` 均可创建 Process Worker carrier 并把 stdout/stderr/exit 回灌到 Kernel ProcessHandle；本轮新增 `bun run <script>` package.json scripts 分发、`pre<script>`/`post<script>` lifecycle 顺序执行、`--` 后参数透传、`npm_lifecycle_*`/`npm_package_*`/`INIT_CWD`/`npm_config_user_agent` 脚本环境和 `node_modules/.bin` PATH lookup，shell、runtime.spawn 与 Bun.spawn 三条入口均通过 MarsShell 执行脚本并回灌 stdout/stderr/exit；自动 worker script loading 与 ServiceWorker 模块拦截待补。 |
| M3-24 | In Progress | 99% | Pass | `mars-lib/packages/mars-sw/`, `mars-lib/packages/mars-kernel/`, `mars-lib/packages/mars-bridge/`, `mars-lib/packages/mars-runtime/`, `mars-lib/packages/mars-client/src/runtime.ts`, `mars-lib/playground/vite.config.ts`, `mars-lib/playground/src/App.tsx` | 串联真实 ServiceWorker 注册、Kernel Worker、Process Worker、模块请求拦截和 bridge 消息流。 | 已补 `@mars/bridge` async request/response、in-memory transport pair 与 postMessage/MessageChannel transport，并新增 SW bridge controller、bridge-backed ServiceWorker kernel client、ServiceWorker fetch event handler、ServiceWorker registration lifecycle 抽象、SW script bootstrap connect、ServiceWorker `/__mars__/module` ESM response、ServiceWorker `/src/*.ts(x)` 源码 URL 首跳、ServiceWorker VFS `node_modules` bare import 二跳、ServiceWorker `sw.vfs.patch` 运行期 VFS 更新、runtime VFS write 到 ServiceWorker bridge 的自动 fanout、Kernel Worker bootstrap connect/controller、页面侧 Kernel Worker `new Worker()` carrier、`kernel.spawn({ kind: "worker" })` 自动创建 Process Worker 并回灌 stdout/exit、Process Worker native carrier、Process Worker stdio protocol、Process Worker lifecycle/VFS patch/run RPC、Process Worker runtime bootstrap context 注入、Process Worker bootstrap script source/Blob URL/snapshot restore 生成和 Process Worker VFS 自动 fanout；验收与 playground 覆盖 client->SW fetch、SW->Kernel `server.request`、MessageChannel Kernel RPC、runtime boot register/ready/client bridge 握手、SW bootstrap 接管 MessagePort、SW ESM entry import 重写和依赖二跳加载、SW `/src/*.ts` 首跳、SW VFS `node_modules` bare import 二跳、嵌套 npm package graph、package exports subpath graph、scoped exports subpath graph、SW bridge patch 后 module fetch、runtime `Bun.write()` 自动 fanout 后 module fetch、Kernel bootstrap 接管 MessagePort、Kernel Worker native carrier 接管 MessagePort、dispose unregister、Kernel->Process Worker boot/message/terminate/patch/run、Kernel spawn 自动 Process Worker、Worker URL native carrier、stdin/stdout/stderr bridge、worker scope 内 `Bun`/`process`/`require` 注入、`bun run <entry>` stdout/exit 回传、worker script URL 生成、真实浏览器 Blob module Worker smoke、Process Worker 增量 VFS patch 后显式 run、Process Worker factory 自动 fanout 后 patch ack、Vite 同源 ServiceWorker script 注册后的 SW scope `/__mars__/module` auto patch+fetch smoke，以及 native `fetch()` 走 ServiceWorker fetch event 的真实 scope smoke 自动化、`@vite/client`/`@vite/env`/`@react-refresh`/`@id/__x00__...`/`@fs/...`/`/node_modules/.vite/deps/...`/`/src/*.ts(x)?import|raw|url` 的 network fallback 和 playground dev/preview COOP/COEP/CORP headers + 页面级 SAB/SW ready 状态面板；SW 生成脚本已移除 top-level await，改用 `bootPromise` 串联 install/activate/fetch/RPC 初始化。npm graph 边缘案例和剩余少量 Vite special path 自动化待补。 |

## Phase 完成标准

- `Bun.spawn()` 可运行受控 Worker 或内置命令，stdio 和退出码稳定。
- `Bun.spawnSync()` 在支持 SAB 的环境下可用，在不支持时有明确可测试的降级行为。
- `Bun.build()` 可通过 `esbuild-wasm` 构建 M2 验收项目核心样例，并写入 MarsVFS。
- M2 运行时路径继续通过 `@swc/wasm-web` 执行 TS/TSX/JSX playground，Phase 3 新增 build 能力不得回退或绕过该验收基线。
- Phase 3 API 切片必须接入 playground；当前 prework 已通过 `playground/vite-react-ts` 覆盖 `Bun.build`，并登记到 `playground/module-cases.json`，但仍不作为 Phase 3 Done 依据。
- `bun run index.ts` 必须经 Shell 命令层创建 Kernel pid，stdout/stderr 通过 ProcessHandle 或 CommandResult 可观测，并登记到 playground 矩阵。
- 真实 ServiceWorker/Kernel Worker/Process Worker 链路必须覆盖启动、请求拦截、bridge 分发、worker 注入、模块加载和退出码回传，未完成前不得把 Phase 3 标记为 Done。
- `Bun.CryptoHasher`、`Bun.password`、`node:crypto` 常用路径通过测试。
- `Bun.sql` 或 sqlite wasm 能在 MarsVFS 上创建、读写并恢复数据库文件。
- WebSocket upgrade 或 HMR 通道不破坏 M2 Vite HMR。
- OPFS 持久化和 snapshot/restore 可恢复工作区。
- Chrome/Firefox 浏览器 profile 验收通过。

## 状态更新规则

1. 每个新增 API 完成时必须更新兼容矩阵。
2. 涉及浏览器/Worker 边界的 Todo，在未覆盖真实浏览器自动化前，完成百分比原则上不超过 90%；若已具备真实浏览器 smoke 且仅剩自动化或边缘路径缺口，可在 90%-99% 区间保守标注，但不得标记为 Done。
3. 涉及 M1/M2 已验收路径的改动，必须重新运行对应 Phase 验收。
4. Phase 总完成百分比按 Todo 完成百分比平均计算。
5. 所有稳定性与兼容性测试通过，并完成对应 playground 入口、playground 矩阵、`module-cases.json` 和兼容文档同步后，Phase 状态才能更新为 `Done`。
